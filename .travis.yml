language: elixir
elixir: '1.7'
node_js: '10'
script:
  - node --version
  - cd assets && yarn install --pure-lockfile && yarn run jest
  - cd ../ && mix test && MIX_ENV=test mix coveralls.travis
branches:
  only:
  - master
