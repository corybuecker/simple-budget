services:
  database:
    image: postgres:18-alpine
    environment:
      POSTGRES_DB: simple_budget
      POSTGRES_HOST_AUTH_METHOD: trust
    volumes:
      - type: bind
        source: migrations/schema.sql
        target: /docker-entrypoint-initdb.d/000-schema.sql
    ports:
      - 5432:5432
